# 水印相机当前已实现功能

本文档仅保留当前项目中已经落地的功能，不包含业务链路、补漏过程、自测过程和后续规划。

## 当前范围

当前实现集中在 Android 原生 Demo，已落地内容以相机预览集成页和独立工具组件为主。

## 已实现功能

### 1. CameraPreviewActivity 集成页

已实现的相机预览集成功能：

* Camera2 实时预览
* 前后摄像头切换
* 闪光灯开关
* 顶部操作栏（返回、闪光灯、翻转镜头）
* 底部操作栏（拍摄按钮、相册按钮、模式 Tab）
* 底部 `CenterTabView` 两项切换 UI（“视频 / 拍照”，默认选中“拍照”）

当前状态说明：

* 已有预览、切换镜头、闪光灯三项基础相机能力
* 拍摄按钮当前仅保留点击入口，未接入真实拍照能力
* 未接入真实水印渲染
* 未接入媒体合成
* 未接入地址位置获取
* 未接入后台水印配置获取
* 未接入城市信息获取

### 2. TiltViewController

已实现的倾斜控制功能：

* 基于重力感应驱动目标 View 状态切换
* 支持旋转与平移联合动画
* 支持状态防抖与最短路径旋转
* 支持生命周期联动注册/注销传感器
* 支持手动开启/停止监听
* 停止监听时自动复位到默认状态

### 3. DialogTopLiftController

已实现的顶部抬升功能：

* 绑定页面内目标 View
* 绑定底部 Dialog
* Dialog 展示时按内容高度驱动目标 View 向上平移
* Dialog dismiss 时自动复位目标 View

### 4. UploadProgressFragment

已实现的上传进度功能：

* 可独立调用的 Fragment 载体
* 自绘水平进度条
* 上传中 / 上传完成状态切换
* 完成态缩略图展示
* 关闭按钮与关闭动画
* 进度重置后重新展示

### 5. CenterTabView

已实现的居中选中 Tab 功能：

* `wrap_content` 文本 item 横向排列
* 选中项始终平移到控件中心
* 文字加粗切换
* 底部圆点指示器
* 点击切换开关控制
* 动画执行期间点击防抖

### 6. PermissionTipView

已实现的权限提示功能：

* 可复用横条提示 View
* 左侧提示文案 + 右侧操作按钮
* 显示/隐藏滑入滑出动画
* 与上传进度区域同位置独立叠放
* 通过回调把“去授权”操作交给外部页面处理

## 当前集成关系

`CameraPreviewActivity` 中当前已经完成的集成关系如下：

* `CameraEngine` 负责相机预览、切换前后摄和闪光灯控制
* `CenterTabView` 负责底部“视频 / 拍照”切换 UI
* `DialogTopLiftController` 负责相册 Dialog 弹出时抬升 `FourCornerView`
* `TiltViewController` 负责 `FourCornerView` 的重力感应旋转和平移
* `UploadProgressFragment` 负责底部上传进度模拟展示
* `PermissionTipView` 负责权限缺失时的横条提示

## 当前文档结论

就当前代码仓库而言，已经真正实现的是：

* 相机预览基础能力
* 前后摄切换
* 闪光灯开关
* 多个独立 UI/交互组件及其集成演示

当前尚未真正实现的是：

* 真实拍照出图
* 水印渲染
* 媒体合成
* 定位与城市信息获取
* 后台水印配置拉取
